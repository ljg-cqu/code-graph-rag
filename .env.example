# ============================================
# Chat/Completion LLM Configuration
# ============================================
#
# These models are used for reasoning tasks: concept extraction, Cypher generation,
# orchestration, query planning, and task splitting.
#
# REQUIREMENTS: Must support instruction following and structured output (JSON).
# DO NOT configure embedding models here — use the Embedding section below.
#
# Role-configured models (ORCHESTRATOR_*, CYPHER_*) automatically appear in /models output.

# Example 1: All Ollama (local)
# ORCHESTRATOR_PROVIDER=ollama
# ORCHESTRATOR_MODEL=llama3.2
# ORCHESTRATOR_ENDPOINT=http://localhost:11434/v1

# CYPHER_PROVIDER=ollama
# CYPHER_MODEL=codellama
# CYPHER_ENDPOINT=http://localhost:11434/v1

# Example 2: All OpenAI
# ORCHESTRATOR_PROVIDER=openai
# ORCHESTRATOR_MODEL=gpt-4o
# ORCHESTRATOR_API_KEY=sk-your-openai-key

# CYPHER_PROVIDER=openai
# CYPHER_MODEL=gpt-4o-mini
# CYPHER_API_KEY=sk-your-openai-key

# Example 3: All Google (AI Studio)
# ORCHESTRATOR_PROVIDER=google
# ORCHESTRATOR_MODEL=gemini-2.5-pro
# ORCHESTRATOR_API_KEY=your-google-api-key

# CYPHER_PROVIDER=google
# CYPHER_MODEL=gemini-2.5-flash
# CYPHER_API_KEY=your-google-api-key
# Example 4: Google Vertex AI
# ORCHESTRATOR_PROVIDER=google
# ORCHESTRATOR_MODEL=gemini-2.5-pro
# ORCHESTRATOR_PROJECT_ID=your-gcp-project-id
# ORCHESTRATOR_REGION=us-central1
# ORCHESTRATOR_PROVIDER_TYPE=vertex
# ORCHESTRATOR_SERVICE_ACCOUNT_FILE=/path/to/service-account.json

# CYPHER_PROVIDER=google
# CYPHER_MODEL=gemini-2.5-flash
# CYPHER_PROJECT_ID=your-gcp-project-id
# CYPHER_REGION=us-central1
# CYPHER_PROVIDER_TYPE=vertex
# CYPHER_SERVICE_ACCOUNT_FILE=/path/to/service-account.json

# Example 5: Mixed - Google orchestrator + Ollama cypher
# ORCHESTRATOR_PROVIDER=google
# ORCHESTRATOR_MODEL=gemini-2.5-pro
# ORCHESTRATOR_API_KEY=your-google-api-key

# CYPHER_PROVIDER=ollama
# CYPHER_MODEL=codellama
# CYPHER_ENDPOINT=http://localhost:11434/v1

# Example 6: Mixed - OpenAI orchestrator + Google cypher
# ORCHESTRATOR_PROVIDER=openai
# ORCHESTRATOR_MODEL=gpt-4o
# ORCHESTRATOR_API_KEY=sk-your-openai-key

# CYPHER_PROVIDER=google
# CYPHER_MODEL=gemini-2.5-flash
# CYPHER_API_KEY=your-google-api-key

# Example 7: Anthropic (native)
# ORCHESTRATOR_PROVIDER=anthropic
# ORCHESTRATOR_MODEL=claude-3-5-sonnet-latest
# ORCHESTRATOR_API_KEY=sk-ant-api03-your-anthropic-key

# CYPHER_PROVIDER=anthropic
# CYPHER_MODEL=claude-3-haiku-latest
# CYPHER_API_KEY=sk-ant-api03-your-anthropic-key

# Example 8: Anthropic-compatible endpoint (e.g., Baidu Qianfan, vLLM)
# ORCHESTRATOR_PROVIDER=anthropic
# ORCHESTRATOR_MODEL=GLM-5
# ORCHESTRATOR_API_KEY=your-custom-api-key
# ORCHESTRATOR_ENDPOINT=https://qianfan.baidubce.com/anthropic/coding

# CYPHER_PROVIDER=anthropic
# CYPHER_MODEL=claude-3-haiku
# CYPHER_API_KEY=your-custom-api-key
# CYPHER_ENDPOINT=https://your-custom-endpoint.com/anthropic

# Thinking budget for reasoning models (optional)
# ORCHESTRATOR_THINKING_BUDGET=10000
# CYPHER_THINKING_BUDGET=5000

# Context Window Configuration
# Default global context window (fallback when model-specific is not detected)
DEFAULT_CONTEXT_WINDOW=200000

# Role-specific context window overrides (highest precedence)
# ORCHESTRATOR_CONTEXT_WINDOW=200000
# CYPHER_CONTEXT_WINDOW=128000

# Provider/model-specific context window overrides
# Format: {PROVIDER}_{NORMALIZED_MODEL_ID}_CONTEXT_WINDOW=<size_in_tokens>
# Normalized model ID: uppercase, spaces/hyphens/periods replaced with underscores
# Examples:
# OPENAI_GPT_4O_CONTEXT_WINDOW=128000
# ANTHROPIC_CLAUDE_3_5_SONNET_CONTEXT_WINDOW=200000
# GOOGLE_GEMINI_2_5_PRO_CONTEXT_WINDOW=1048576
# OLLAMA_LLAMA3_1_CONTEXT_WINDOW=128000

# Memgraph settings
# SECURITY WARNING: In production, always change of default credentials below
# and bind MEMGRAPH_HOST to localhost only (never 0.0.0.0) to prevent unauthorized access
MEMGRAPH_HOST=localhost
MEMGRAPH_PORT=7687
MEMGRAPH_HTTP_PORT=7444
# Memgraph authentication credentials
# Leave MEMGRAPH_USERNAME empty (or omit it) if your Memgraph instance doesn't require authentication
# If authentication is enabled, provide both username and password
# SECURITY: You MUST set these values explicitly - default credentials are disabled
# MEMGRAPH_USERNAME=admin
# MEMGRAPH_PASSWORD=your_secure_password_here
LAB_PORT=3000
MEMGRAPH_BATCH_SIZE=1000
MEMGRAPH_CONNECTION_TIMEOUT=600  # Connection timeout in seconds (10 minutes)
# Python object inspection timeout (seconds)
PYTHON_INSPECT_TIMEOUT=10
# Shell command execution timeout (seconds)
SHELL_COMMAND_TIMEOUT=30
MEMGRAPH_QUERY_MAX_RETRIES=3  # Number of retry attempts for failed queries

# Transaction conflict retry settings (for parallel relationship flushing)
# When multiple workers write to overlapping nodes, Memgraph may throw
# "Cannot resolve conflicting transactions". These settings control automatic retry.
MEMGRAPH_CONFLICT_RETRY_ENABLED=true
MEMGRAPH_CONFLICT_RETRY_MAX_ATTEMPTS=3
MEMGRAPH_CONFLICT_RETRY_BASE_DELAY=0.1  # Base delay in seconds for exponential backoff

# Repository settings
TARGET_REPO_PATH=.
# Number of parallel workers to use for codebase indexing (default: 20)
# Auto-optimized at runtime: will never exceed available CPU cores or number of changed files
# Set to 1 to disable parallel indexing entirely (run sequentially)
# Uses perfect round-robin load distribution across workers for balanced throughput
PARALLEL_INDEXING_WORKERS=20

# Timeout in seconds for worker processes during indexing (default: 3600 = 1 hour)
# Prevents indefinite hangs when processing large files or stuck workers
# Increase for very large codebases with many complex files
INDEXING_WORKER_TIMEOUT=3600

# ============================================
# Session Behavior Settings
# ============================================

# Yolo mode: Auto-approve all tool operations without confirmation
# When enabled, file edits, shell commands, and other operations are auto-approved
# Warning: Use with caution on production codebases
# CLI flags --yolo or --no-confirm take precedence over this setting
CGR_YOLO_MODE=false

# Quiet mode: Suppress non-essential output (progress messages, banners)
CGR_QUIET=false

# ============================================
# Logging Configuration
# ============================================
# All logs are written to terminal by default

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL, default: INFO)
CGR_LOG_LEVEL=DEBUG

# Ollama base URL (without /v1 suffix)
# OLLAMA_BASE_URL=http://localhost:11434

# ============================================
# Vector Store Backend Configuration
# ============================================

# Memgraph native vector storage is always used
# All embeddings are stored directly on graph nodes for atomic hybrid vector+graph queries
# No external vector database dependencies required
MEMGRAPH_VECTOR_INDEX_NAME=code_embeddings  # Used for logging/identification
MEMGRAPH_VECTOR_DIM=768              # Must match embedding model (768 for UniXcoder)
MEMGRAPH_VECTOR_CAPACITY=100000      # REQUIRED - estimate ~2x your function count
MEMGRAPH_VECTOR_METRIC=cos           # Options: l2sq, cos, ip, pearson
MEMGRAPH_VECTOR_SCALAR_KIND=f32      # Options: f32, f64, f16, bf16, f8

# ============================================
# Graph Algorithm Configuration
# ============================================

# Enable/disable automatic post-ingestion algorithm runs (default: true)
# Runs ANALYZE GRAPH, PageRank, and Community Detection after every ingestion
ALGORITHM_RUN_POST_INGESTION=true

# Enable PageRank calculation (improves retrieval ranking by identifying important nodes)
ALGORITHM_ENABLE_PAGERANK=true

# Enable Community Detection (groups related code entities, improves global query performance)
ALGORITHM_ENABLE_COMMUNITY_DETECTION=true
# Community detection algorithm to use: "leiden" (higher quality, default) or "louvain" (faster for large graphs)
ALGORITHM_COMMUNITY_ALGORITHM=leiden

# Embedding Cache (backend-agnostic)
# Stores embedding computation cache for faster subsequent runs
EMBEDDING_CACHE_DIR=./.embedding_cache

# ============================================
# Embedding Model Configuration
# ============================================
#
# These models are used for vector generation: document/code embeddings,
# semantic similarity search, and vector index operations.
#
# REQUIREMENTS: Must support text -> vector transformation ONLY.
# DO NOT configure chat models here — they cannot generate embeddings.
# DO NOT use these models for concept extraction — they cannot follow instructions.
#
# Default: Local UniXcoder (no API key needed)
# IMPORTANT: For local embeddings, install the semantic extra:
#   uv sync --extra semantic
# This installs torch and transformers. Without these, document indexing
# will fall back to structural-only mode (no semantic search).
EMBEDDING_PROVIDER=local
EMBEDDING_MODEL=microsoft/unixcoder-base
EMBEDDING_DEVICE=auto  # auto, cpu, or cuda

# Alternative: Use cloud providers (no torch/transformers needed)
# EMBEDDING_PROVIDER=openai
# EMBEDDING_MODEL=text-embedding-3-small
# EMBEDDING_API_KEY=your-openai-key
# MEMGRAPH_VECTOR_DIM=1536  # Must match embedding dimension

# Embedding fallback behavior (when configured provider fails)
# EMBEDDING_FALLBACK_TO_LOCAL=true  # Fall back to local model on API failure (default: true)
# EMBEDDING_FALLBACK_MODEL=BAAI/bge-base-en-v1.5  # Local fallback model (768 dim, matches default)
# Note: For document indexing, use CGR_DOC_EMBEDDINGS_REQUIRED=false to continue without any embeddings

# Example: OpenAI embeddings
# EMBEDDING_PROVIDER=openai
# EMBEDDING_MODEL=text-embedding-3-small
# EMBEDDING_API_KEY=sk-your-openai-key

# Example: Aliyun/Dashscope embeddings (OpenAI-compatible API)
# EMBEDDING_PROVIDER=openai
# EMBEDDING_MODEL=text-embedding-v4
# EMBEDDING_API_KEY=sk-your-aliyun-key
# EMBEDDING_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# MEMGRAPH_VECTOR_DIM=1024  # text-embedding-v4 uses 1024 dimensions
# EMBEDDING_FALLBACK_TO_LOCAL=false  # UniXcoder fallback is 768d and will not match this configuration

# Example: Ollama local embeddings
# EMBEDDING_PROVIDER=ollama
# EMBEDDING_MODEL=nomic-embed-text
# EMBEDDING_KEEP_ALIVE=5m

# Example: Google embeddings
# EMBEDDING_PROVIDER=google
# EMBEDDING_MODEL_MODEL=text-embedding-004
# EMBEDDING_API_KEY=your-google-api-key

# Unified Vector Settings (apply to both backends)
VECTOR_SEARCH_TOP_K=5
VECTOR_EMBEDDING_BATCH_SIZE=50

# ============================================
# Document GraphRAG Configuration
# ============================================

# Document graph database (separate instance for isolation)
# SECURITY WARNING: In production, always change of default credentials below
# and bind DOC_MEMGRAPH_HOST to localhost only (never 0.0.0.0) to prevent unauthorized access
DOC_MEMGRAPH_HOST=localhost
DOC_MEMGRAPH_PORT=7688
# Default credentials: replace with your own secure password before production use
DOC_MEMGRAPH_USERNAME=admin
DOC_MEMGRAPH_PASSWORD=REPLACE_WITH_SECURE_PASSWORD_IN_PRODUCTION
DOC_MEMGRAPH_MEMORY_LIMIT=4GB
DOC_MEMGRAPH_CONNECTION_TIMEOUT=600  # Connection timeout in seconds (10 minutes)
DOC_MEMGRAPH_CONNECTION_RETRY_ATTEMPTS=3  # Connection retry attempts on transient failures
DOC_MEMGRAPH_CONNECTION_RETRY_BASE_DELAY=1.0  # Base delay in seconds for connection retries
DOC_INCREMENTAL_FLUSH_INTERVAL=10  # Flush every N documents (0 = disable incremental flush)

# Document graph Lab UI (Memgraph Lab for document graph)
DOC_LAB_PORT=3001

DOC_MEMGRAPH_VECTOR_INDEX_NAME=doc_embeddings
DOC_MEMGRAPH_VECTOR_CAPACITY=100000
DOC_VECTOR_SEARCH_TOP_K=5



# Document indexing settings
DOC_ENABLED=true
DOC_SUPPORTED_EXTENSIONS=[".md", ".rst", ".txt", ".pdf", ".docx"]
DOC_ENABLE_PDF_EXTRACTION=true
DOC_MAX_FILE_SIZE_MB=50
DOC_EXTRACTION_TIMEOUT_SECONDS=30
# Maximum number of chunks per document to prevent memory exhaustion
DOC_MAX_CHUNKS_PER_DOCUMENT=5000

# Document embedding mode (graceful degradation when embeddings unavailable)
# When DOC_EMBEDDINGS_REQUIRED=false (default), document indexing continues without embeddings
# if the embedding provider is unavailable (e.g., missing transformers/torch)
# When DOC_EMBEDDINGS_REQUIRED=true, indexing fails if embeddings cannot be generated
CGR_DOC_EMBEDDINGS_ENABLED=true
CGR_DOC_EMBEDDINGS_REQUIRED=false

# ============================================
# Document Concept Extraction Configuration
# ============================================
# Enable/disable LLM-based concept extraction from document chunks (default: true)
# When enabled, extracts key concepts and relationships from documents for graph-based queries
DOC_CONCEPT_EXTRACTION_ENABLED=true

# Minimum confidence threshold for concept extraction (0.0-1.0, default: 0.7)
# Concepts with confidence below this threshold are filtered out
DOC_CONCEPT_MIN_CONFIDENCE=0.7

# Maximum concurrent LLM calls for concept extraction (1-50, default: 10)
# Higher values speed up extraction but increase API costs
DOC_CONCEPT_EXTRACTION_CONCURRENCY=10

# Max retries for concept extraction per chunk (0-10, default: 3)
DOC_CONCEPT_EXTRACTION_MAX_RETRIES=3

# Base delay between concept extraction retries in seconds (0.1-60, default: 1.0)
DOC_CONCEPT_EXTRACTION_RETRY_DELAY=1.0

# Timeout multiplier after a timeout error (1.0-5.0, default: 1.5)
# Each retry gets timeout = previous_timeout * this_value, capped at DOC_CONCEPT_MAX_TIMEOUT
DOC_CONCEPT_TIMEOUT_RETRY_MULTIPLIER=1.5

# Delay multiplier after a timeout error (1.0-10.0, default: 3.0)
# Timeout retries wait longer: delay = max(base_delay * this_value, base_delay * 2^attempt)
DOC_CONCEPT_TIMEOUT_RETRY_DELAY_MULTIPLIER=3.0

# Base timeout for concept extraction (seconds, default: 30, range: 5-300)
DOC_CONCEPT_BASE_TIMEOUT=30.0

# Maximum timeout cap for concept extraction (seconds, default: 120, range: 30-600)
# Large or complex chunks may exceed base timeout; this is the absolute ceiling
DOC_CONCEPT_MAX_TIMEOUT=120.0

# Additional timeout per 1000 characters (seconds, default: 10, range: 0-30)
DOC_CONCEPT_TIMEOUT_PER_1K_CHARS=10.0

# Additional timeout per code block (seconds, default: 5, range: 0-15)
DOC_CONCEPT_TIMEOUT_PER_CODE_BLOCK=5.0

# Circuit breaker: enable/disable protection for concept extraction LLM calls (default: true)
CGR_CIRCUIT_BREAKER_ENABLED=true

# Circuit breaker failures before opening (default: 5, range: 1-20)
CGR_CIRCUIT_FAILURE_THRESHOLD=5

# Circuit breaker successes to close from half-open (default: 2, range: 1-10)
CGR_CIRCUIT_SUCCESS_THRESHOLD=2

# Circuit breaker recovery timeout in seconds (default: 60, range: 10-300)
CGR_CIRCUIT_TIMEOUT_SECONDS=60.0

# Circuit breaker rolling window size for failure rate calculation (default: 10, range: 1-50)
CGR_CIRCUIT_WINDOW_SIZE=10

# ============================================
# Document Graph Algorithm Configuration
# ============================================
# Maximum path depth for graph traversal algorithms (1-10, default: 5)
# Controls the maximum hop distance for shortest path and neighbor queries
DOC_GRAPH_MAX_PATH_DEPTH=5

# Maximum number of neighbors to return in related concepts queries (1-500, default: 50)
DOC_GRAPH_MAX_NEIGHBORS=50

# ============================================
# JSON Graph Configuration
# ============================================
# JSON graph database (separate instance for isolation)
# SECURITY WARNING: In production, always change of default credentials below
# and bind JSON_MEMGRAPH_HOST to localhost only (never 0.0.0.0) to prevent unauthorized access
JSON_MEMGRAPH_HOST=localhost
JSON_MEMGRAPH_PORT=7689
# Default credentials: replace with your own secure password before production use
JSON_MEMGRAPH_USERNAME=admin
JSON_MEMGRAPH_PASSWORD=REPLACE_WITH_SECURE_PASSWORD_IN_PRODUCTION
JSON_MEMGRAPH_MEMORY_LIMIT=2GB
JSON_MEMGRAPH_CONNECTION_TIMEOUT=600  # Connection timeout in seconds (10 minutes)

# JSON graph Lab UI (Memgraph Lab for JSON graph)
JSON_LAB_PORT=3002

JSON_MEMGRAPH_VECTOR_INDEX_NAME=json_embeddings
JSON_MEMGRAPH_VECTOR_CAPACITY=100000
JSON_VECTOR_SEARCH_TOP_K=5

# JSON ingestion parallel worker count (uses round-robin connection pool for load balancing)
# Adjust based on your workload and Memgraph instance capacity
JSON_PARALLEL_WORKERS=10

# JSON embedding settings
# Set CGR_JSON_EMBEDDINGS_ENABLED=false to disable JSON embeddings entirely
# Set CGR_JSON_EMBEDDINGS_REQUIRED=true to fail ingestion when embeddings are unavailable
CGR_JSON_EMBEDDINGS_ENABLED=true
CGR_JSON_EMBEDDINGS_REQUIRED=false

# ============================================
# Parallel Sub-Agent Worker LLM Configuration
# ============================================
# Configure dedicated LLMs for parallel sub-agent workers
# If no worker LLMs are configured, sub-agents automatically use the orchestrator LLM
# The dedicated Cypher LLM remains unchanged and is shared by all workers for graph queries

# Example 1: Simple comma-separated format (uses default provider credentials for each model)
# CGR_WORKER_LLMS="openai:gpt-4o-mini,anthropic:claude-3-haiku,ollama:llama3.2"

# Example 2: Full JSON array format (specify custom credentials/endpoints per model)
# CGR_WORKER_LLMS='[
#     {"provider": "openai", "model_id": "gpt-4o-mini", "api_key": "sk-your-key", "endpoint": "https://api.openai.com/v1"},
#     {"provider": "anthropic", "model_id": "claude-3-haiku-20240307", "api_key": "sk-ant-your-key"},
#     {"provider": "ollama", "model_id": "llama3.2", "endpoint": "http://localhost:11434/v1"}
# ]'

# LLM assignment strategy for sub-agents: only "round-robin" is supported currently
# Distributes configured LLMs evenly across workers for optimal load balancing
CGR_WORKER_LLM_ASSIGNMENT_STRATEGY=round-robin

# ============================================
# Model Catalog Management Configuration
# ============================================
# Custom model catalog file path (JSON/YAML). When set, catalog is loaded from this file
# instead of the static MODEL_CATALOG. File must match ModelInfo schema.
# CGR_MODEL_CATALOG_PATH=/path/to/custom_catalog.json

# Disable dynamic model discovery from .env configuration (ORCHESTRATOR_*, CYPHER_*, CGR_WORKER_LLMS).
# When true, only the static (or external) catalog is shown in /models output.
# CGR_DISABLE_MODEL_DISCOVERY=false

# ============================================
# Automatic Parallel Execution Configuration
# ============================================
# Enable/disable automatic parallel execution detection (no explicit user request needed)
# When enabled, system automatically detects parallelizable tasks and activates 10 round-robin workers
# to speed up execution by up to 10x, no user intervention required
CGR_AUTO_PARALLEL_ENABLED=true

# Default number of parallel workers (fixed to 10 as per default configuration)
# Uses strict round-robin task assignment for even load distribution
CGR_DEFAULT_PARALLEL_WORKERS=10

# Maximum allowed parallel workers (override with CGR_ALLOW_DYNAMIC_MAX_OVERRIDE=true)
CGR_MAX_PARALLEL_WORKERS=20

# Allow overriding of max worker limit for large workloads
CGR_ALLOW_DYNAMIC_MAX_OVERRIDE=true

# Automatically scale worker count to match number of subtasks (no more workers than subtasks)
CGR_AUTO_SCALE_WORKERS=true

# Confidence threshold for automatic parallel activation (0.0-1.0)
# Only activates parallel when detection confidence is >= this value
CGR_PARALLEL_ELIGIBILITY_THRESHOLD=0.6

# Model to use for simple subtasks (filtering, sorting, counting) - defaults to orchestrator model
CGR_SIMPLE_TASK_MODEL=openai:gpt-4o-mini

# Enable deduplication of overlapping results from multiple subtasks
CGR_AGGREGATION_DEDUPLICATION_ENABLED=true

# Enable detailed metrics logging for parallel execution
CGR_PARALLEL_METRICS_ENABLED=true

# Maximum size of parallel task queue, falls back to sequential when full
CGR_PARALLEL_MAX_QUEUE_SIZE=200

# ─────────────────────────────────────────────────────────
# Enhanced Parallel Execution Configuration
# ─────────────────────────────────────────────────────────

# Strict write detection mode (default: false)
# When true, uses current broad regex (more false positives, more conservative)
# When false, uses enhanced Layer 1-4 detection with contextual understanding
CGR_PARALLEL_WRITE_DETECTION_STRICT=false

# Minimum subtasks required for parallelization (default: 2, min: 1)
CGR_PARALLEL_MIN_SUBTASKS=2

# Enable adaptive threshold adjustment (default: true)
# Replaces confidence-multiplier calibration with threshold-based adjustment
CGR_PARALLEL_ADAPTIVE_THRESHOLD=true

# Enable file type hint extraction for task splitting (default: true)
# Adds Strategy 2 (language/category filtering) to _collect_scoped_files()
CGR_PARALLEL_FILE_TYPE_HINTS=true

# Include codebase context in LLM eligibility analysis (default: true)
# Adds file count, languages, repo size to LLM system prompt
CGR_PARALLEL_CODEBASE_CONTEXT=true

# Force parallel execution override (CLI flag only, no env var)
# Use --force-parallel on the command line; not configurable via environment

# Timeout per subtask execution (seconds)
CGR_SUBAGENT_TIMEOUT=300

# Allow sub-agents to perform write operations (default: false for safety)
# Write operations are always executed sequentially to prevent corruption
CGR_SUBAGENT_ALLOW_WRITE=false

# Enable automatic task splitting for parallel execution
CGR_AUTO_SPLIT_ENABLED=true

# Number of retry attempts for failed subtasks
CGR_SUBAGENT_RETRY_ATTEMPTS=2

# Maximum LLM requests per agent.run() session (default: None = unlimited)
# pydantic-ai enforces a default cap of 50 requests without this override
AGENT_REQUEST_LIMIT= 1000

# ============================================
# Debug / Troubleshooting Configuration
# ============================================

# Force re-check of pydantic_ai availability on every check_pydantic_ai() call
# Useful for debugging import issues after installing packages in a running process
# CGR_FORCE_PYDANTIC_AI_RECHECK=false

# ============================================
# Realtime Updater Configuration
# ============================================
# Enable/disable realtime file watching globally (CLI flags take precedence)
# CGR_REALTIME_UPDATER=false

# Debounce delay in seconds - waits for quiet period after last change (default: 5.0)
# CGR_REALTIME_DEBOUNCE=5.0

# Maximum wait time in seconds before processing changes (default: 30.0)
# CGR_REALTIME_MAX_WAIT=30.0

# Enable realtime updates for code files (default: true)
# CGR_REALTIME_CODE=true

# Enable realtime updates for document files (default: false)
# Requires --with-docs or --index-docs flag to be effective
# CGR_REALTIME_DOCS=false

# Enable realtime updates for JSON files (default: false)
# CGR_REALTIME_JSON=false

# ============================================
# Intelligent Context Window Compression
# ============================================
# Automatically prevents LLM context window overflow with zero semantic loss for critical content
# Uses 10 parallel round-robin workers to select optimal compression strategy per scenario
# All features are enabled by default with optimal production settings, no configuration needed

# Percentage of context window usage that triggers automatic compression (default: 85.0)
# Runs before any LLM call that would exceed limit, preventing failure entirely
# CONTEXT_COMPRESSION_AUTO_TRIGGER_PCT=85.0

# Hysteresis buffer percentage to avoid repeated compression cycles when context hovers near limit (default:5.0)
# Only recompression only runs again when usage drops below (trigger - hysteresis) then rises back to trigger
# CONTEXT_COMPRESSION_HYSTERESIS_PCT=5.0

# Minimum acceptable semantic retention score percentage (default: 70.0)
# If best compression strategy achieves lower than this score, automatically roll back to original context
# CONTEXT_COMPRESSION_MIN_RETENTION_SCORE=70.0

# Number of parallel round-robin workers to use for strategy evaluation (default: 10)
# Use 20 for higher throughput on large workloads
# CONTEXT_COMPRESSION_PARALLEL_WORKERS=10

# Minimum retention threshold for aggressive compression mode (default: 50.0)
# Lower values allow higher compression ratios with lower semantic retention
# CONTEXT_COMPRESSION_AGGRESSIVE_RETENTION_THRESHOLD=50.0

# How long to keep compressed context archives for restore capability (hours, default: 24)
# Archives are automatically deleted after this time to save storage
# CONTEXT_COMPRESSION_ARCHIVE_TTL_HOURS=24

# Enable/disable context compression entirely (default: true)
# CONTEXT_COMPRESSION_ENABLED=true

# ============================================
# Query Method Optimization Configuration
# ============================================
# These settings control the enhanced query orchestrator for improved
# multi-paradigm retrieval with adaptive sequencing and graceful degradation.

# Minimum confidence score for intent-based method selection (0.0-1.0, default: 0.5)
# Below this threshold, all available query methods are executed
# CGR_QUERY_INTENT_CONFIDENCE_THRESHOLD=0.5

# Minimum number of successful methods required before early termination (1-6, default: 2)
# Higher values ensure more comprehensive results at the cost of latency
# CGR_QUERY_MIN_METHODS=2

# Number of consecutive failures before a query method is temporarily disabled (1-10, default: 3)
# Prevents wasted effort on consistently failing backends
# CGR_QUERY_CIRCUIT_BREAKER_THRESHOLD=3

# Number of top results to spot-check for graph-to-disk integrity (1-20, default: 5)
# Verifies file existence and line number bounds for retrieved results
# CGR_QUERY_INTEGRITY_CHECK_COUNT=5

# Enable graph data integrity verification (default: true)
# Checks that graph metadata matches actual source files on disk
# CGR_QUERY_ENABLE_INTEGRITY_CHECK=true

# Enable template-based Cypher fallback when LLM generation fails (default: true)
# Uses pre-built parameterized queries for common patterns
# CGR_QUERY_TEMPLATE_FALLBACK_ENABLED=true

# ============================================
# LLM Rate Limiting and Quota Management
# ============================================
# Rate limiting prevents quota exhaustion and provides automatic fallback between providers

# Enable rate limiting for LLM calls (default: true)
# CGR_RATE_LIMIT_ENABLED=true

# Default rate limit: requests per minute (default: 60)
# CGR_RATE_LIMIT_RPM=60

# Maximum burst size for rate limiting (default: 10)
# CGR_RATE_LIMIT_BURST=10

# Quota warning threshold: ratio before WARNING status (0.0-1.0, default: 0.80)
# CGR_QUOTA_WARNING_THRESHOLD=0.80

# Quota critical threshold: ratio before CRITICAL status (0.0-1.0, default: 0.95)
# CGR_QUOTA_CRITICAL_THRESHOLD=0.95

# Primary LLM provider for orchestration and queries (overrides auto-detection)
# CGR_PRIMARY_LLM_PROVIDER=openai
# CGR_PRIMARY_LLM_MODEL=gpt-4o

# Fallback providers when primary is unavailable or quota exhausted
# Comma-separated list of provider names (e.g., "openai,anthropic,google")
# CGR_FALLBACK_PROVIDERS=anthropic,google

# Automatically fallback to alternate providers when quota exhausted (default: true)
# CGR_FALLBACK_ON_QUOTA_EXHAUSTED=true

# Provider-specific rate limits (requests per minute)
# CGR_OPENAI_RPM=100
# CGR_ANTHROPIC_RPM=50
# CGR_DOUBAO_RPM=60
# CGR_GOOGLE_RPM=60
# CGR_OLLAMA_RPM=1000

# ============================================
# JSON Ingestion Filtering Configuration
# ============================================
# Pre-filtering reduces I/O and log noise by skipping non-CGR JSON files

# Enable JSON file filtering (default: true)
# CGR_JSON_FILTER_ENABLED=true

# Skip default filter patterns and use only custom patterns (default: false)
# CGR_JSON_FILTER_SKIP_DEFAULTS=false

# Additional glob patterns to exclude from JSON ingestion
# Comma-separated list (e.g., "**/test_data/**/*.json,**/fixtures/**/*.json")
# CGR_JSON_FILTER_CUSTOM_PATTERNS=
